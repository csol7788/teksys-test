bucket          = "state-bucket-name" # This is assumed built when AWS account is commissioned.
region          = "deploy-region" # sample region
key             = "webapp/deploy-region/webapp-prod.tfstate" # Terraform state file - stored within S3
kms_key_id      = "KMS Key ARN" # State File KMS encryption key
dynamodb_table  = "State-locking-tablename" # State File locking DynamoDB Table Name.